import React from "react";

import FormTry from "../src/Register/Login/Register.jsx";

function Test() {
  return (
    <div>
      <FormTry />
    </div>
  );
}

export default Test;
//there is no need to export the component as default, as it is not used in any other file
//but it is a good practice to do so
